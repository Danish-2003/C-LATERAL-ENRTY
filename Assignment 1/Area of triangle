#include <bits/stdc++.h>
using namespace std;

int main(){
    //Heron's formula= sqrt(s*(s-a)*(s-b)*(s-c));
    int a=5;
    int b=5;
    int c=5; 
    float s=((a+b+c)/2);
    float sum=sqrt(s*(s-a)*(s-b)*(s-c));
    cout<<sum;



    return 0;
}
